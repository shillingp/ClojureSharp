namespace TestNamespace
{
    int ExampleMethod(int a, int b)
    {
        //int foo = 6;
        //return 10 + foo + a * b;

        List<int> thing = new List<int>(1, 2, 3);
        
        // thing.Add(1);

        return thing;
    }

    int thing(int a)
    {
        return a + 1;
    }
    
    ExampleMethod(((((1 + 2)))), 2);
}
