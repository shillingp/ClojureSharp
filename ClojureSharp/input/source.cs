namespace TestNamespace
{
    int ExampleMethod(int a, int b)
    {
        int foo = 6;
        int bar = null;

        ExampleMethod(1, 2);

        bar = 10;
        
        return 10 + a * b;
    }

    ExampleMethod((((1 + 2))), 2);
}
